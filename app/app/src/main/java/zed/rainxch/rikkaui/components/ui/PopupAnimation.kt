package zed.rainxch.rikkaui.components.ui

public enum class PopupAnimation {
    /** Fade + vertical expand from top edge (default). */
    FadeExpand,

    /** Simple fade in/out with no size change. */
    Fade,

    /** Instant show/hide, no animation. */
    None,
}
