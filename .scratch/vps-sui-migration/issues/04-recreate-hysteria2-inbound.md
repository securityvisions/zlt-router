# 04 — Recreate the Hysteria2 inbound

**What to build:** The Hysteria2 inbound on 31800 with its three clients using their existing auth strings, salamander obfuscation, and a self-signed TLS certificate — replacing the broken empty-cert config. A Hysteria2 client connects and egresses through the VPS.

**Blocked by:** 02

**Status:** ready-for-agent

- [ ] The Hysteria2 inbound exists on 31800 with the three clients using their existing auth strings
- [ ] Salamander obfuscation and a self-signed TLS certificate are configured
- [ ] A Hysteria2 client connection succeeds and egresses via the VPS IP
