# 02 — Xirouter app scaffold + router connection

**What to build:** The Android project (Compose, Room, serialization, mirroring chandtoman's build), a first-run setup screen (router address default 192.168.1.1 + token), the API client, and a Dashboard rendering Status data with a 'cached as of' footer and offline fallback. Demo: sideload the APK, enter the token, see live router status.

**Blocked by:** 01

**Status:** ready-for-agent

- [ ] APK builds and installs
- [ ] Setup screen saves address + token; Dashboard shows live status from the router
- [ ] Persian RTL UI with Persian digits
