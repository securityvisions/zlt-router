# 11 — Actions & app lock

**What to build:** Router Test, Proxy switch and Reboot endpoints (guarded), the app's Actions screen with confirm dialogs, and the optional PIN app lock gating destructive actions. Switching the proxy node from the app flips the bot's Proxy card.

**Blocked by:** 01, 02

**Status:** ready-for-agent

- [ ] /test, /proxy/switch and /reboot work (reboot is a no-op off the router)
- [ ] App Actions screen runs URL tests, switches nodes, reboots with confirm
- [ ] App lock gates destructive actions
