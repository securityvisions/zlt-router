# 08 — Local history + balance-drain chart

**What to build:** The app's Room Local history (a sample on every poll, 12-month retention, pruning), the hand-rolled Compose Canvas chart foundation with Persian digits, and the first chart — the balance-drain trend merging router history and local samples. The drain curve draws even with the router unreachable.

**Blocked by:** 02, 05, 07

**Status:** ready-for-agent

- [ ] Room stores balance + usage samples and prunes to 12 months
- [ ] Line chart renders the drain curve from merged router + local history
- [ ] Chart math is unit-tested
